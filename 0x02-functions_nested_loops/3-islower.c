#include "main.h"
#include <ctype.h>

/**
 * int _islower(int c) - checks whether the letter is lowercase or uppercase
 *
 * Return: 0
 */
int _islower(int c)
{
	char mina = m;
	int a = 49;
	int b = 48;

	if (islower(mina))
{
	_putchar(a);
}
else 
{
	 _putchar(b);
}
	_putchar(*"\n");
	return (0);

}
