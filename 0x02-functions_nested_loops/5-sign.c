#include "main.h"

/**
 * print_sign - prints sign of a number
 * @c : used as parameter
 *
 * Return : 0
 */

int print_sign(int n)
{
if (n > 0)
{
	return (+);
}
else if (n = 0)
{
	return (0);
}
else
{
	return (-);
}
}
