#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes the absolute value
 * @c : used as parameter
 * Return: 0 or 1
 */

int _abs(int c)
{
	int d = abs(c);

	return (d);
}
