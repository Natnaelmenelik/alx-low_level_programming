#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 *
 * @c : serves as parameter
 * Return: 0 or 1
 */

int print_last_digit(int c)
{
	int d = c % 10;
	int e = c % -10;

	return (e);
	return (d);
	_putchar(*"\n");
}
