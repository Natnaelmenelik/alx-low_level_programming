#include "main.h"

/**
 * _isupper - checks for uppercase characters
 * @c - serves as parameter
 *
 * Return: 0 or 1
 */

int _isupper(int c)

{
if (c >= 'A' &&  C <= 'Z')
{
	return (1);
}
else 
{
	return (0);
}
}
