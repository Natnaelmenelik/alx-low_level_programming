#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * string_toupper - changes all lower case to upper
 * @str : string input
 * Return: 0 or 1
 */

char *string_toupper(char *str)
{
	int i = 0;

	while (stri[i])
	{
	_putchar(toupper(str[i]));
	i++;
	}

	return (0);

}
