#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _print_rev_recursion - prints reverse in string
 * @s : the string
 * Return: reversed string
 */

void _print_rev_recursion(char *s)
{
	strrev(s);
}
