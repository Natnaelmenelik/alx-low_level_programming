#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - prints length of a string
 * @s : string
 * Return : 0
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
