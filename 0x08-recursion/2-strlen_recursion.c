#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - prints length of a string
 * @s : string
 * Return : lenth of a string
 */

int _strlen_recursion(char *s)
{
	_putchar(strlen(s));
}
